var names = ["Dylan"];
names.push("Jack");
console.log(names);
var numbers = [1, 2, 3]; // inferred to type number[]
numbers.push(4); // no error
console.log(numbers);
var head = numbers[0]; // no error
console.log(head);
